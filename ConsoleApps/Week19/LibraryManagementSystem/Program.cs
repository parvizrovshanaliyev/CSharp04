using LibraryManagementSystem.Managers;

namespace LibraryManagementSystem
{
    internal class Program
    {
        static void Main(string[] args)
        {
            /*
             * Step 1: Initialize Managers
             * - Create AuthenticationManager to handle user login/authentication
             * - Create LibraryManager to manage library items (books, magazines, articles)
             */

            /*
             * Step 2: User Authentication Flow
             * - Display welcome message
             * - Handle user login with retry attempts
             * - Show appropriate success/failure messages
             */

            /*
             * Step 3: Main Application Menu
             * - Show different options based on authentication status
             * - Options include:
             *   > Search items
             *   > Add new items 
             *   > Delete items
             *   > View all items
             *   > Logout
             */

            /*
             * Step 4: Library Operations
             * - Handle user input for selected operations
             * - Validate inputs before processing
             * - Display operation results
             * - Return to main menu after each operation
             */

            /*
             * Step 5: Session Management
             * - Track login status
             * - Handle user logout
             * - Clean up resources
             * - Exit application
             */

           
        }
    }
}
